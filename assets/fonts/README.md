# Bundled fonts

These vendored TTFs keep the site deterministic and independent of remote font
providers. Web pages load Space Grotesk from this directory through
`next/font/local`; image scripts embed the same files for downloadable SVGs and
use pinned fontkit glyph outlines for raster PNG inputs. Sharp receives path
geometry with no text nodes or font definitions, so host Pango and Fontconfig
versions cannot alter the generated bytes.

The default social image and downloadable identity kit are static generated
assets. Regenerate all of them with `pnpm brand:generate`, or only the default
Open Graph image with `pnpm generate:og`; there is no dynamic image route or
runtime font fetch.

| Family                              | Source                                          | License     |
| ----------------------------------- | ----------------------------------------------- | ----------- |
| Space Grotesk (Regular/Medium/Bold) | https://github.com/floriankarsten/space-grotesk | SIL OFL 1.1 |
| JetBrains Mono (Regular/Bold)       | https://github.com/JetBrains/JetBrainsMono      | SIL OFL 1.1 |
