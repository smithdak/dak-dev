---
version: 1
slug: 'app-work-page-tsx'
primary_target: 'app/work/page.tsx'
related_targets: ['components/work']
---

## Scope and mode

`/work`. Visitor mode: Experience.

## Audience, job, and action

Readers evaluating Dakota's practice need to inspect the existing systems and understand the recurring mechanisms across them. The primary action is opening an external project; the secondary action is filtering the complete ledger by system type.

## Content and constraints

Use only `content/products.json` names, dates, categories, descriptions, feature flags, live URLs, and optional repository URLs. Do not fabricate case-study outcomes, clients, screenshots, adoption, or production status. Distinguish a live system from its public source repository whenever both exist.

## Chosen direction

Systems ledger. Featured work receives oversized numbered case records with the mechanism made legible through the existing description; the full catalog follows as a compact filterable index. It avoids a portfolio-card grid and keeps every project visible. The memorable moment is the cumulative numbered record: a body of systems, not a shelf of logos.

## Unresolved decisions

None. The user explicitly delegated frontend direction.
