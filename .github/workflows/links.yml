name: Link Check

on:
  schedule:
    - cron: '0 12 * * 1' # Weekly on Monday at noon UTC
  pull_request:
    paths:
      - 'content/posts/**'

permissions:
  contents: read

jobs:
  check-links:
    name: Check Links
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Check links with Lychee
        id: lychee
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2
        with:
          # --base-url resolves root-relative links (e.g. /blog/<slug>) against the
          # production site so internal links are checked over HTTP instead of failing
          # with "Cannot convert path ... provide a root dir" (false positives, issue #35).
          args: '--verbose --no-progress --base-url https://daksmith.dev content/posts/*.mdx'
          fail: false

      - name: Post PR comment (on PR)
        if: github.event_name == 'pull_request' && steps.lychee.outputs.exit_code != '0'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const fs = require('fs');
            let report = '';
            if (fs.existsSync('./lychee/out.md')) {
              report = fs.readFileSync('./lychee/out.md', 'utf8');
            }
            const body = `## Link Check Results\n\nSome links may be broken:\n\n${report}\n\n---\n*Generated by link-check workflow*`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });

      - name: Create/update issue (on schedule)
        if: github.event_name == 'schedule' && steps.lychee.outputs.exit_code != '0'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const fs = require('fs');
            let report = '';
            if (fs.existsSync('./lychee/out.md')) {
              report = fs.readFileSync('./lychee/out.md', 'utf8');
            }
            const title = 'Broken links detected';
            const labels = ['broken-links'];
            const body = `## Weekly Link Check Report\n\nDate: ${new Date().toISOString().split('T')[0]}\n\n${report}\n\n---\n*Auto-generated by link-check workflow*`;

            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: labels.join(','),
              state: 'open',
            });

            if (issues.length > 0) {
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issues[0].number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels,
              });
            }
