#!/usr/bin/env -S pnpm exec tsx
/**
 * @deprecated Compatibility entry point for deterministic manifest-driven art.
 * The canonical command is `pnpm images:generate`.
 */

void import('./generate-post-images');
